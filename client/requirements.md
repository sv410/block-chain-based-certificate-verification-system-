## Packages
framer-motion | Page transitions and scroll-triggered animations for a premium feel
date-fns | Easy formatting of issue dates
lucide-react | High quality icons for the UI

## Notes
Dark mode is explicitly enforced via CSS variables to meet the "clean minimal dark mode professional" requirement.
Verify endpoint uses POST as defined in the schema, so it will be implemented as a useMutation hook.
